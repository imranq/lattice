{
  "name": "lattice",
  "version": "0.1.0",
  "private": true,
  "description": "Concepts, partially ordered \u2014 a prerequisite graph and warm-up ladders over a hard problem bank.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "cloud:build": "node cloud/build.mjs",
    "cloud:deploy": "node cloud/build.mjs && (cd cloud/api && npx wrangler deploy) && (cd cloud/pages && npx wrangler pages deploy --branch main)"
  },
  "engines": {
    "node": ">=22.5"
  }
}
